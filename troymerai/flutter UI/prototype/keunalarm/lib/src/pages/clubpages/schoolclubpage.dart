import 'package:flutter/material.dart';

class schoolClubPage extends StatelessWidget {
  const schoolClubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('중앙 동아리 페이지')),
    );
  }
}
