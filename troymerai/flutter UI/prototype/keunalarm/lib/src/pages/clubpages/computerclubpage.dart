import 'package:flutter/material.dart';

class computerClubPage extends StatelessWidget {
  const computerClubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('컴퓨터 공학과 연구 동아리 목록')),
    );
  }
}
